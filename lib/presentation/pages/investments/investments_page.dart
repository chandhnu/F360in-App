import 'package:flutter/material.dart';

class InvestmentsPage extends StatelessWidget {
  const InvestmentsPage();

  @override
  Widget build(BuildContext context) => Scaffold(appBar: AppBar(title: const Text('Investments')), body: const Center(child: Text('Investments (Coming Soon)')));
}
